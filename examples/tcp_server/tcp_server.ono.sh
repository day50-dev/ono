#!/bin/bash
# TCP Server for Ono - AI-powered templating language processor

<?ono generate python code to start a TCP server on port 8080 that processes ono blocks and returns results ?>

# Usage:
#   bash tcp_server.ono.sh
#   echo '[?ono your query?]' | nc localhost 8080
